CREATE TABLE PlayerEntity (
    id TEXT NOT NULL PRIMARY KEY,
    name TEXT NOT NULL UNIQUE,
    scores TEXT NOT NULL DEFAULT ""
);

insert:
INSERT OR REPLACE INTO PlayerEntity(id, name, scores)
VALUES (?, ?, ?);

selectByName:
SELECT * FROM PlayerEntity
WHERE name = ?;

selectAll:
SELECT * FROM PlayerEntity;
