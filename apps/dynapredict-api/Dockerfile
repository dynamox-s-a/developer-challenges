# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build dynapredict-api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t dynapredict-api`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system dynapredict-api && \
          adduser --system -G dynapredict-api dynapredict-api

COPY dist/apps/dynapredict-api dynapredict-api/
RUN chown -R dynapredict-api:dynapredict-api .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix dynapredict-api --omit=dev -f install

CMD [ "node", "dynapredict-api" ]
