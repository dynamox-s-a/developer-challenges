# Configuração do Banco de Dados
# Copie este arquivo para .env e preencha com suas credenciais

# Para desenvolvimento local (Docker Compose)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/dynamox

# Para produção (Railway/outras plataformas)
# A variável DATABASE_URL será fornecida automaticamente pela plataforma
# Não é necessário configurar aqui em produção

# Exemplo de DATABASE_URL para PostgreSQL:
# DATABASE_URL=postgresql://usuario:senha@host:porta/nome_do_banco

# Exemplo de DATABASE_URL para Railway:
# Railway fornece automaticamente via variável de ambiente DATABASE_URL

