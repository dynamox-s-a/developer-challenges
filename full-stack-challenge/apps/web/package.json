{
  "name": "@dynamox/web",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  }
}
