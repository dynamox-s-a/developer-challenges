// Importa os matchers customizados do Testing Library (ex: toBeInTheDocument)
import '@testing-library/jest-dom'

// (Opcional) Mock global do fetch, se você não quiser chamadas reais
// import 'whatwg-fetch' // se precisar de polyfill
// global.fetch = jest.fn()

// (Opcional) Configuração adicional, como servidor MSW, etc.
