export enum MachineType {
    Pump = "Pump",
    Fan = "Fan"
}

export enum SensorModel {
    HF_PLUS = "HF_PLUS",
    TCAG = "TCAG",
    TCAS = "TCAS"
}

export enum snackType {
    success = "success",
    error = "error",
    info = "info",
    warning = "warning"
}