{
  "name": "@dynamox/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Componentes de estado reutilizáveis (carregando, erro, vazio) sobre Material UI 5.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --max-warnings 0",
    "test": "echo \"@dynamox/ui: coberto pelos testes de apps/web\" && exit 0"
  },
  "peerDependencies": {
    "@mui/material": "^5.16.7",
    "react": "^18.3.1"
  },
  "devDependencies": {
    "@mui/material": "^5.16.7",
    "@types/react": "^18.3.12",
    "react": "^18.3.1"
  }
}
