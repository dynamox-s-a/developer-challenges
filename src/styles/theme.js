import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: '#263252',
      light: '#F4F7FC'
    },
  },
  typography: {
    fontFamily: 'Raleway, sans-serif',
    fontSize: 20
  },
})