import styled from "styled-components";

export const ErrorMessage = styled.div`
  color: var(--red-100);
  font-size: 0.8rem;
  margin-top: 3%;
  display: flex;
  align-items: center;
  gap: 0.2rem;
`;
