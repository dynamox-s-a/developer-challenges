{
  "name": "@dynamox/sensor-twin",
  "version": "0.2.0",
  "private": true,
  "description": "BON-06: gêmeo digital determinístico do sensor HF+ na bomba P-101. Gera ciclos sintéticos de telemetria e os ingere pela API local usando o mesmo contrato e validador do backend. Dados sintéticos e didáticos; nenhuma afiliação ou diagnóstico físico real.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --max-warnings 0",
    "test": "jest --runInBand",
    "test:integration": "jest --runInBand --config jest.integration.config.js",
    "test:ros": "jest --runInBand --config jest.ros.config.js"
  },
  "dependencies": {
    "@dynamox/contracts": "0.1.0",
    "@dynamox/domain": "0.1.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5"
  }
}
