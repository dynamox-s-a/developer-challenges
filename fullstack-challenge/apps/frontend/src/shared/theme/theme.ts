import { createTheme } from "@mui/material"

export const Theme = createTheme({
  palette: {
    primary: {
      main: '#501833',
      light: '#702147',
      contrastText: '#ffffff'
    },
    background: {
      default: '#F4F4F4',
      paper: '#ffffff',
    }
  }
});