import styles from "./Header.module.css"

const Header = () => {
    return (
        <div className={styles.Header}>
            <h1 className={styles.Header_title}>Autenticação - Cadastro de Produto</h1>
        </div>
    )
}

export default Header;
