import { Stack, StackProps } from '@mui/material'
import { styled } from '@mui/material/styles'

export const AlertWrapper = styled(Stack)<StackProps>({
  marginTop: 0,
  marginLeft: 24,
  marginRight: 24
})
